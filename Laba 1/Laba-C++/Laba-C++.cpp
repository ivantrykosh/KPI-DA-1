#include "Func.h"
#include "function.h"

int main()
{
	// The basic algorithm
	//int t1 = time(NULL);
	////createfile();
	//start();
	//int t2 = time(NULL);
	//cout << "\nThe time of the program's work: " << t2 - t1 << "s" << endl;

	// The modified algorithm
	//long long int n = 1310720; // 10 MB
	//long long int n = 20; // A-test
	//time_t t1 = time(NULL);
	////Createfile(n);
	//Start(n);
	//readfile("Files/bin/A.bin");
	//readfile("Files/bin/C1.bin");
	//time_t t2 = time(NULL);
	//cout << "\nThe time of the program's work: " << t2 - t1 << "s" << endl;
	return 0;
}